<%- include("templates/header") %>

    <div class="container my-5">
        <h1>💸 Your Financials</h1>

        <input type="number" id="moneyInput" placeholder="Enter your current money" class="form-control mb-4" />

        <div class="plan-options">
            <input type="radio" name="plan" value="70% Spend / 30% Save" id="plan1">
            <label for="plan1">70% Spend / 30% Save</label>
            <input type="radio" name="plan" value="60% Spend / 40% Save" id="plan2">
            <label for="plan2">60% Spend / 40% Save</label>
            <input type="radio" name="plan" value="50% Spend / 50% Save" id="plan3">
            <label for="plan3">50% Spend / 50% Save</label>
        </div>

        <button id="aiMagicTrigger" class="btn btn-warning mb-3" onclick="triggerGeminiAI()">🔮 Ask AI for Financial
            Advice</button>

        <div id="aiResults" class="alert alert-info d-none"></div>

        <div class="chart-container mb-5">
            <h3>💡 Spending Analysis</h3>
            <canvas id="moneyChart" width="400" height="400"></canvas>
        </div>
    </div>
    <!-- 📊 Mobile-Optimized Spending Tracker -->
    <div class="card p-3 mb-4">
        <h4 class="mb-3 text-center">📊 Track Your Spending</h4>
        <form id="spendingForm">
            <div class="mb-3">
                <label for="spendAmount" class="form-label">💰 Amount Spent</label>
                <input type="number" class="form-control" id="spendAmount" placeholder="e.g., 50" required />
            </div>
            <div class="mb-3">
                <label for="spendDate" class="form-label">📅 Date of Spending</label>
                <input type="date" class="form-control" id="spendDate" required />
            </div>
            <button type="submit" class="btn btn-success w-100">💾 Save</button>
        </form>
    </div>

    <div class="mb-4">
        <label for="viewFilter" class="form-label fw-semibold">📅 View Mode</label>
        <select id="viewFilter" class="form-select">
            <option value="week">Weekly</option>
            <option value="biweek">Bi-Weekly</option>
            <option value="month">Monthly</option>
            <option value="year">Yearly</option>
        </select>
    </div>

    <div class="chart-container mb-5">
        <h3 class="text-center mb-3">📈 Spending History</h3>
        <canvas id="spendingChart" class="w-100" style="max-height: 400px;"></canvas>
    </div>



    <%- include("templates/footer") %>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <script src="/scripts/money.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

        <%- include("templates/navbar") %>